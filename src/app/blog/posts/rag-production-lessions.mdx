---
title: "LLM을 프로덕션에서 운영하며 배운 것들"
publishedAt: "2025-01-15"
summary: "KB국민은행 AI 서비스를 운영하면서 실제로 마주친 문제들. 청크 전략, 프롬프트 관리, 모니터링."
image: "/images/icon/llmops.jpg"
---

"RAG 구현했다"랑 "RAG를 프로덕션에서 운영한다"는 전혀 다른 이야기입니다.

## 1. 청크 크기가 성능을 결정한다

금융 FAQ 도메인에서는 질문-답변 단위로 청크를 구성하는 게 단순 문자 수 기반 분할보다 훨씬 효과적이었습니다.

```python
def chunk_by_qa(document: str) -> list[str]:
    """Q: / A: 패턴으로 분할 — 의미 단위로 청킹"""
    chunks = []
    qa_pairs = re.split(r'\n(?=Q:)', document)
    for pair in qa_pairs:
        if pair.strip():
            chunks.append(pair.strip())
    return chunks
```

## 2. 프롬프트는 버전 관리해야 한다

```python
v1.0 (2024-01)
  당신은 KB국민은행 고객 상담 AI입니다.
v1.4 (2024-04) — 할루시네이션 이슈로 규칙 추가
  모르는 정보는 지어내지 마세요. 담당 직원에게 안내하세요.
v1.7 (2024-07) — 민원 케이스 반영
  투자 수익 예측이나 보장은 하지 마세요.
```

<Feedback
  icon
  variant="warning"
  title="변경 이유를 반드시 기록하세요"
  description="언제 어떤 이유로 바꿨는지 기록해두지 않으면 나중에 왜 이 규칙이 있는지 알 수 없습니다. 프롬프트도 코드처럼 버전 관리가 필요합니다."
/>

## 3. 모니터링 항목

<Table
  data={{
    headers: [
      { content: "항목", key: "item" },
      { content: "확인하는 것", key: "check" }
    ],
    rows: [
      ["검색 유사도 분포", "임계값 이하 비율 — 높으면 지식베이스 보강 필요"],
      ["TTFT (첫 토큰까지 시간)", "사용자 체감 응답 속도"],
      ["부정 피드백 질의", "실패 패턴 파악 → 프롬프트·청크 개선"],
      ["일일 토큰 사용량", "API 비용 추적 및 이상 탐지"]
    ]
  }}
/>

## 4. 스트리밍은 선택이 아니다

```typescript
const stream = await groq.chat.completions.create({
  model: "llama-3.3-70b-versatile",
  messages: [...],
  stream: true,
});

for await (const chunk of stream) {
  const text = chunk.choices[0]?.delta?.content || "";
  controller.enqueue(encoder.encode(`data: ${JSON.stringify({ text })}\n\n`));
}
```

실험 환경에서 잘 됐던 게 프로덕션에서 예상치 못한 방식으로 실패하는 경우가 있습니다. 그 간극을 메우는 게 엔지니어링입니다.
